apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "britbus-realtime.fullname" . }}-test-connection"
  labels:
    {{- include "britbus-realtime.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "britbus-realtime.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
