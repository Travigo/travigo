apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "travigo-events.fullname" . }}-test-connection"
  labels:
    {{- include "travigo-events.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "travigo-events.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
