apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "travigo-basic-consumer.fullname" . }}-test-connection"
  labels:
    {{- include "travigo-basic-consumer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "travigo-basic-consumer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
