apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "britbus-web-api.fullname" . }}-test-connection"
  labels:
    {{- include "britbus-web-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "britbus-web-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
